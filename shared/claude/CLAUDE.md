@~/.claude/RTK.md

## Effort table — task type → model (machine-wide, layer-neutral)
Canonical source is version-controlled in the vault and imported here, so it loads into **every** repo/session (personal, creator, employer, unrelated coding) while its history lives in the vault's git. Pure model routing — no identity content, so the import crosses no layer wall. Edit tiers in that file only.

@~/code/aios-vault/AIOS/Systems/effort-table.md

## Reasoning doctrine (machine-wide, layer-neutral)
Standing cognitive procedures (intent-reading, fact re-derivation, certainty marking, self-attack, final gate) distilled from Fable 5 before its deprecation. Canonical in the vault, no identity content, imported everywhere like the effort table. Edit rules in that file only.

@~/code/aios-vault/AIOS/Systems/reasoning-doctrine.md

## AIOS brain vault
The AIOS brain vault lives at `~/code/aios-vault` (run `claude` from there, or use the `aios` alias, to auto-load it via its `CLAUDE.md` + SessionStart hook). From other directories, read `~/code/aios-vault/CLAUDE.md` on demand ONLY when the task is personal / creator / AIOS work — do not pull it into unrelated coding sessions, to keep the identity layers separate.
# graphify
- **graphify** (`~/.claude/skills/graphify/SKILL.md`) - any input to knowledge graph. Trigger: `/graphify`
When the user types `/graphify`, use the installed graphify skill or instructions before doing anything else.

## Ponytail amendment (machine-wide, layer-neutral)
Personal dead-code-asymmetry rule extending the ponytail plugin. Canonical in the vault (survives plugin upgrades), no identity content, imported everywhere like [[effort-table]] and [[reasoning-doctrine]]. Edit the rule in that file only.

@~/code/aios-vault/AIOS/Systems/ponytail-amendment.md

## Plan-recon amendment (machine-wide, layer-neutral)
Personal extension to the superpowers `writing-plans` skill: recon order is graph → grep → read → write, a brief cites only what it has opened, and cross-task seams get named. Canonical in the vault (survives plugin upgrades), no identity content, imported everywhere like [[ponytail-amendment]]. Edit the rule in that file only.

@~/code/aios-vault/AIOS/Systems/plan-recon-amendment.md
